{% set isMin = '.min' %}
{% if this.isToolbarDebug() %}
    {% set isMin = '' %}
{% endif %}
{# @TODO gvf #}
{#{% if bb.token != null %}#}
    <link href="{{ this.getResourceUrl('toolbar/dist/css/bb-ui' ~ isMin ~ '.css') }}" rel="stylesheet">
    <link href="{{ this.getResourceUrl('toolbar/dist/css/vendor' ~ isMin ~ '.css') }}" rel="stylesheet">
{#{% else %}#}
    {#<link href="{{ this.getResourceUrl('toolbar/dist/css/bb-ui-login' ~ isMin ~ '.css') }}" rel="stylesheet">#}
{#{% endif %}#}

{% if disableToolbar == false %}
<script src="{{ this.getResourceUrl('toolbar/dist/require.js') }}"></script>

<div id="{{ wrapper }}"
    data-api="{{ this.getUri('/rest/2/') }}"
    data-base-url="{{ this.getUri('/') }}"
    data-toolbar-selector="true"
     {# @TODO gvf #}
    {#{% if bb.token != null %}#}
        data-autostart
        data-site-uid="{{ this.getCurrentPage().getSite().getUid() }}"
        data-page-uid="{{ this.getCurrentPage().getUid() }}"
        data-layout-uid="{{ this.getCurrentPage().getLayout().getUid() }}"
        data-root-uid="{{ this.getCurrentPage().getRoot().getUid() }}"
    {#{% endif %}#}
>
    <div id="backbee-loader">
        <div class="loader">
            <div id="circularG_1" class="circularG"></div>
            <div id="circularG_2" class="circularG"></div>
            <div id="circularG_3" class="circularG"></div>
            <div id="circularG_4" class="circularG"></div>
            <div id="circularG_5" class="circularG"></div>
            <div id="circularG_6" class="circularG"></div>
            <div id="circularG_7" class="circularG"></div>
            <div id="circularG_8" class="circularG"></div>
        </div>
    </div>
    
    <script src="{{ this.getResourceUrl('toolbar/dist/config' ~ isMin ~ '.js') }}"></script>
</div>
{% endif %}